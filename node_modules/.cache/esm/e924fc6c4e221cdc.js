let minimist,now,forecast,help,configure,version;_4e2‍.x([["cli",()=>cli]]);_4e2‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_4e2‍.w("./now",[["now",["now"],function(v){now=v}]]);_4e2‍.w("./forecast",[["forecast",["forecast"],function(v){forecast=v}]]);_4e2‍.w("./help",[["help",["help"],function(v){help=v}]]);_4e2‍.w("./configure",[["configure",["configure"],function(v){configure=v}]]);_4e2‍.w("./version",[["version",["version"],function(v){version=v}]]);






       async function cli(argsArray) {
  const args = minimist(argsArray.slice(2));
  let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;

    case 'now':
      now(args);
      break;

    case 'forecast':
      forecast(args);
      break;

    case 'config':
      configure(args);
      break;

    default:
      console.error(`"${cmd}" is not a valid command!`);
      break;
  }
}